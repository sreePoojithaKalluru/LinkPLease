fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.36
alembic>=1.13.3
httpx>=0.27.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1
aiosqlite>=0.20.0
# Test dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
# asyncpg is only needed when DATABASE_URL points to Postgres.
# Render uses Python 3.11 where asyncpg wheels are available.
# Uncomment if deploying locally against Postgres (requires Python <=3.12):
# asyncpg==0.29.0
